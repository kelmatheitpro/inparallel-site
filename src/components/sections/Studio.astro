---
import Section from "@/components/Section.astro";
import Heading from "@/components/Heading.astro";
import ArrowLink from "@/components/inparallel/ArrowLink.astro";
import { STUDIO_CARDS } from "@/data/home.ts";
---

<Section id="studio" theme="light" align="center">
  <Heading tag="h2" variant="h1" class="heading-split inline studio_title">
    One crew, zero handoffs, <em>fiercely honest</em>
  </Heading>

  <div class="studio_grid">
    {
      STUDIO_CARDS.map((card) => (
        <article class="studio_card">
          {/* The design leaves these two frames empty — the studio and team
              shots drop in here. */}
          <div class="studio_media" aria-hidden="true" />
          <div class="studio_body">
            <h3 class="studio_card_title">{card.title}</h3>
            <p class="studio_text">{card.text}</p>
          </div>
          <ArrowLink href={card.href}>{card.linkLabel}</ArrowLink>
        </article>
      ))
    }
  </div>
</Section>

<style is:global>
  @layer components {
    .studio_title {
      text-align: center;
    }

    .studio_grid {
      width: 100%;
      display: grid;
      gap: var(--space-8);
      grid-template-columns: repeat(auto-fit, minmax(min(24rem, 100%), 1fr));
    }

    /* The heading is centred by the section; the cards under it are not. */
    .studio_card {
      display: flex;
      flex-direction: column;
      align-items: start;
      gap: var(--space-5);
      text-align: start;
    }

    .studio_media {
      width: 100%;
      height: 25rem;
      background-color: rgb(0 0 0 / 0.2);
      border: var(--border-width) solid var(--border-soft);
      border-radius: var(--radius-card);
    }

    .studio_body {
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
    }

    .studio_card_title {
      font-size: var(--h4);
      font-weight: var(--primary-medium);
      line-height: var(--line-height-small);
      letter-spacing: var(--letter-spacing-tight);
    }

    .studio_text {
      color: var(--text-muted);
      line-height: var(--line-height-large);
    }
  }
</style>
